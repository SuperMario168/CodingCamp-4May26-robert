# Tech Stack

> This project is in early setup. Update this file as the stack is confirmed.

## Current Status
No build system, framework, or dependencies have been committed yet. The stack will be determined as the spec is implemented.

## Expected Stack (to be confirmed)
Based on the project type (interactive dashboard), likely candidates include:

- **Frontend**: React, Vue, or plain HTML/CSS/JS
- **Styling**: Tailwind CSS or a component library
- **State management**: Context API, Zustand, or similar
- **Backend** (if needed): Node.js / Express or a serverless approach
- **Data persistence**: LocalStorage, IndexedDB, or a lightweight backend DB

## Common Commands

> Fill in once the project is scaffolded.

```bash
# Install dependencies
npm install

# Start development server
npm run dev

# Run tests
npm test

# Build for production
npm run build
```

## Notes
- Prefer well-known, actively maintained packages with pinned versions
- Avoid introducing new libraries without updating this file
